The incubator contains the first, POC implementation of the dust core components
(kernel and lab), until the lab can carry itself. The exact project distribution
is irrelevant, the components later will exist as Unit and Module objects inside
the lab...
